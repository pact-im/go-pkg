A file with a syntax error. The loader treats a parse error as fatal, since it
could hide a //plumb: directive, so Load never returns a package — there is no
golden.
-- config --
load-error: true
-- go.mod --
module example.com/app

go 1.23
-- app.go --
package app

func A( { }
