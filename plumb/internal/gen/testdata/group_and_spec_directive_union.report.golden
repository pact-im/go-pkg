plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (2 providers, 2 instantiations):
  var X in() → out(val A)
  var Y in() → out(val B)
  inject build in() → out(val A, val B)

set "test" (1 provider, 1 instantiation):
  var X in() → out(val A)
  inject test in() → out(val A)
