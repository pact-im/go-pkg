A provider directive on an embedded struct field is rejected.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrEmbeddedField
-- example.com/app/a.go --
package app

type Base struct{ X int }

type Conf struct {
	//plumb:build
	Base
}
