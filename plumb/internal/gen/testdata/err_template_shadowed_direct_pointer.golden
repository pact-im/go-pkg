example.com/app/a.go:9:6: unused result-generic template: provider Tmpl in set "build": *app.Node[int] is already produced by Direct, so no demand is left for it: remove the template or the other provider
