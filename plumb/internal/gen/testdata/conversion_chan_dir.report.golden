plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (4 providers, 4 instantiations):
  func NewSignal in() → out(val chan struct{})
  convert chan<- struct{} in(chan struct{}) → out(val chan<- struct{})
  convert <-chan struct{} in(chan struct{}) → out(val <-chan struct{})
  func Use in(chan<- struct{}, <-chan struct{}) → out(val int)
  inject build in() → out(val int)
