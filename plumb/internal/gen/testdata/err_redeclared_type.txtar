A struct-type provider directive on the losing declaration of a redeclared type fails
loudly instead of silently vanishing with the missing object.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrInvalidType
-- example.com/app/a.go --
package app

type T struct{}

//plumb:build
type T struct{ N int }
