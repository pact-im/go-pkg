An unexported type in a cross-package provider signature is unreachable from the destination.
-- config --
import-path: example.com/wire
package-name: wire
error-is: ErrUnreachableType
-- example.com/ext/ext.go --
package ext

type secret struct{}

//plumb:build
func New() *secret { return nil }
