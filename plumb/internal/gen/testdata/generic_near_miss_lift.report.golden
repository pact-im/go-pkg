plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (3 providers, 3 instantiations):
  func Conv in(U) → out(val Box[Name])
  func UseName in(Box[Name]) → out(val int)
  func UseInt in(Box[int]) → out(val string)
  inject build[U any] in(U, Box[int]) → out(val int, val string)
