Two providers produce the same type; plumb never guesses a winner.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrAmbiguousProducer
-- example.com/app/a.go --
package app

type T struct{}

//plumb:build
func A() *T { return nil }

//plumb:build
func B() *T { return nil }
