// plumb:build (with a space) is an ordinary comment, not a directive.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrNoDirectives
-- example.com/app/a.go --
package app

type T struct{}

// plumb:build
func A() *T { return nil }
