Channel and map provider value types: a channel consumed as an input and a map
produced as an output, both rendered into the generated signature.
-- config --
import-path: example.com/app
package-name: app
-- example.com/app/app.go --
package app

type Event struct{}
type Server struct{}

//plumb:build
func NewServer(events chan Event) *Server { return &Server{} }

//plumb:build
func Stats(s *Server) map[string]int { return nil }
