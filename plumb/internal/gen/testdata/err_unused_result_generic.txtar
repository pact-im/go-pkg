A result-generic template whose type parameter is never pinned by demand is unusable.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrUnusedTemplate
-- example.com/app/a.go --
package app

type Box[T any] struct{}

//plumb:build
func NewBox[T any]() *Box[T] { return nil }
