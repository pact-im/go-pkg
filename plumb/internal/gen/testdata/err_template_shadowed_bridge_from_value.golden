example.com/app/a.go:9:6: unused result-generic template: provider Tmpl in set "build": *app.Node[int] is already covered via the value/pointer bridge by Leaf (which produces app.Node[int]), so no demand is left for it: remove the template or the other provider
