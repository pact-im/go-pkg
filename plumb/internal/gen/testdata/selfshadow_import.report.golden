plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (1 provider, 1 instantiation):
  func useReplacer in(strings) → out(val *strings2.Replacer)
  inject build in(strings) → out(val *strings2.Replacer)
  imports:
    strings2 "strings"
