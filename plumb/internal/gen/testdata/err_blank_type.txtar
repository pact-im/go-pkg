A //plumb directive on a blank-named type is rejected: a blank identifier cannot
be named, so it can host no struct provider.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrBlankProvider
-- example.com/app/a.go --
package app

//plumb:build
type _ struct{ X int }
