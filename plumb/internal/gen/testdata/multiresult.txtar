A single provider returning many results of different roles (two bare cleanups,
an error, and two values) exercises result classification and cleanup
aggregation from one call.
-- config --
import-path: example.com/app
package-name: app
-- example.com/app/app.go --
package app

type T struct{}
type U struct{}

//plumb:build
func F() (func(), *U, error, func(), *T) { return nil, nil, nil, nil, nil }
