A provider returning two values of the same type is an ambiguous producer of it.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrAmbiguousProducer
-- example.com/app/a.go --
package app

type T struct{}

//plumb:build
func A() (*T, *T) { return nil, nil }
