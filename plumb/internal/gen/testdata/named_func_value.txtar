Only the bare func()/func() error shapes are special. A named function type is an
ordinary value output, not a cleanup.
-- config --
import-path: example.com/app
package-name: app
output: plumb_gen.go
-- example.com/app/app.go --
package app

type Handler func()

//plumb:build
func NewHandler() Handler { return func() {} }
