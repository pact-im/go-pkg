A provider directive on an embedded interface is rejected.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrEmbeddedInterface
-- example.com/app/a.go --
package app

type Base interface{ M() }

type Reporter interface {
	//plumb:build
	Base
}
