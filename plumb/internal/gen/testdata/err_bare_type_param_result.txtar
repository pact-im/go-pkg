A provider whose result is a bare type parameter cannot be matched to a demand.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrBareTypeParamResult
-- example.com/app/a.go --
package app

//plumb:build
func Default[T any]() T { var z T; return z }

//plumb:build
func Use(x int) bool { return true }
