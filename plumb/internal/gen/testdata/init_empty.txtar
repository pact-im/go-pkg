A set named init with an empty inferred signature is allowed: it generates a
valid func init() that wires package-level side effects.
-- config --
import-path: example.com/app
package-name: app
output: plumb_gen.go
-- example.com/app/app.go --
package app

//plumb:init
func Register() {}
