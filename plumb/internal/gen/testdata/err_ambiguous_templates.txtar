Two templates can both produce the demanded instantiation: ambiguous.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrAmbiguousTemplates
-- example.com/app/a.go --
package app

type Box[T any] struct{}
type Sink struct{}

//plumb:build
func MakeA[T any]() *Box[T] { return nil }

//plumb:build
func MakeB[T any]() *Box[T] { return nil }

//plumb:build
func NewSink(b *Box[int]) *Sink { return nil }
