plumb: scanned 2 package(s): example.com/app, example.com/ext
plumb: generating into package "example.com/app"

set "build" (2 providers, 2 instantiations):
  func NewServer in() → out(val *Server)
  func GetURL in() → out(val *url2.URL)
  inject build in() → out(val *Server, val *url2.URL)
  imports:
    "example.com/ext"
    url2 "net/url"
