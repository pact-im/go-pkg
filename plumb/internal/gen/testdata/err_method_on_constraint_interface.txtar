A method provider on a constraint (type-set) interface has no value receiver.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrConstraintInterfaceMethod
-- example.com/app/a.go --
package app

type Num interface {
	~int | ~float64
	//plumb:build
	Zero() int
}
