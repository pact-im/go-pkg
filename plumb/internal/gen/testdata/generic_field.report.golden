plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (3 providers, 3 instantiations):
  func NewFoo in() → out(val Foo[int])
  field Foo.Bar in(Foo[int]) → out(val Baz[int])
  func NewSink in(Baz[int]) → out(val *Sink)
  inject build in() → out(val *Sink)
