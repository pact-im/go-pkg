plumb: scanned 1 package(s): example.com/app
plumb: generating into package "example.com/app"

set "build" (3 providers, 3 instantiations):
  func Make in() → out(val Box[Name])
  func UseGood in(Box[Name]) → out(val int)
  func UseBad in(Box[int]) → out(val string)
  inject build in(Box[int]) → out(val int, val string)
