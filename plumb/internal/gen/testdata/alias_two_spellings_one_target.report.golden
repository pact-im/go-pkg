plumb: scanned 1 package(s): example.com/lib
plumb: generating into package "example.com/app"

set "build" (1 provider, 1 instantiation):
  func New in(map[lib.A1]lib.A2) → out(val *lib.Thing)
  inject build in(map[lib.A1]lib.A2) → out(val *lib.Thing)
  imports:
    "example.com/lib"
