example.com/app/a.go:9:6: unused result-generic template: provider MakeBox in set "build": app.Box[int] is already produced by ConcreteBox, so no demand is left for it: remove the template or the other provider
