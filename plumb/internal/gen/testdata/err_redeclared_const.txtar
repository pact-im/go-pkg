A //plumb: directive on the losing declaration of a redeclared constant fails loudly
instead of silently vanishing with the missing object.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrInvalidType
-- example.com/app/a.go --
package app

const C int = 1

//plumb:build
const C int = 2
