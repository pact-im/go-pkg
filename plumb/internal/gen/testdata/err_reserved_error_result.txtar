A set named after a predeclared identifier the body emits unqualified (error) is rejected.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrShadowsPredeclared
-- example.com/app/a.go --
package app

type T struct{}

//plumb:error
func New() (*T, error) { return &T{}, nil }
