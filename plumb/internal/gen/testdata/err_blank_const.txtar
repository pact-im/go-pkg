A //plumb: directive on a blank constant declares nothing to wire and is
rejected, rather than silently doing nothing.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrBlankProvider
-- example.com/app/a.go --
package app

type Port int

//plumb:build
const _ Port = 8080
