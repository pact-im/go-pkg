Uncommon types rendered in the generated signature: a fixed-size array and
instantiated generic types with one and two type arguments. As injector inputs
they pass through the type renderer (unlike intermediates, which are inferred).
-- config --
import-path: example.com/app
package-name: app
-- example.com/app/app.go --
package app

type Box[T any] struct{}
type Pair[K, V any] struct{}
type Cfg struct{}
type Out struct{}

//plumb:build
func F(a [2]*Cfg, b Box[string], c Pair[int, string]) *Out { return &Out{} }
