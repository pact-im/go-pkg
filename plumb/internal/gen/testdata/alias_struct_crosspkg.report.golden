plumb: scanned 1 package(s): example.com/core
plumb: generating into package "example.com/app"

set "build" (2 providers, 2 instantiations):
  struct Config in(core.Host) → out(val core.Config)
  func NewServer in(core.Config) → out(val *core.Server)
  inject build in(core.Host) → out(val *core.Server)
  imports:
    "example.com/core"
