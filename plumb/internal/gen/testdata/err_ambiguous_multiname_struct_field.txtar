Both names in a multi-name struct-field group are providers; two of the same type collide.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrAmbiguousProducer
-- example.com/app/a.go --
package app

type C struct {
	//plumb:build
	A, B int
}

//plumb:build
func NewC() C { return C{} }

//plumb:build
func Use(n int) string { return "" }
