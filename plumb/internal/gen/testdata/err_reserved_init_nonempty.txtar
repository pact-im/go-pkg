A set named init with a non-empty signature would generate an illegal func init.
-- config --
import-path: example.com/app
package-name: app
error-is: ErrReservedName
-- example.com/app/a.go --
package app

type T struct{}

//plumb:init
func A(x int) *T { return nil }
