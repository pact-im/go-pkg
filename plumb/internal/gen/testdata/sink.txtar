A side-effect-only provider (no results) alongside a fallible one: the generated
function calls the sink without assignment and returns only an error.
-- config --
import-path: example.com/app
package-name: app
-- example.com/app/app.go --
package app

type Config struct{}
type Server struct{}

//plumb:build
func NewServer(c *Config) (*Server, error) { return &Server{}, nil }

//plumb:build
func Register(s *Server) {}
